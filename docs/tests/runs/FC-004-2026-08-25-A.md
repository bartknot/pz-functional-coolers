# FC-004 Run Record — 2026-08-25 — Run A

## Identity

- Task: `FC-004`
- Run date: 2026-08-25
- Run identifier: `FC-004-2026-08-25-A`
- Run role: replacement Run A
- Classification: `VALID`
- Assessed by: Test Analyst
- Protocol reference: the matched, counterbalanced selected/active-versus-unselected protocol in `CURRENT_TASK.md`

This valid run replaces the earlier protocol-invalidated Run A attempt that lacked the required final `END` marker.

## Runtime Baseline

Directly logged runtime facts:

- Project Zomboid Build `42.20.3 70207f62e0`
- `DayLength = 4`
- `FoodRotSpeed = 3`
- `FridgeFactor = 3`
- Functional Coolers Test Harness `v0.4.3-dev`
- Harness setup version `11`
- Harness control path `FC004_INVENTORY_CONTEXT_MENU`

The runtime log does not emit a Git commit. The Test Engineer separately verified that the deployed `v0.4.3-dev` Lua source matched the reviewed repository working-tree source byte-for-byte before this run. At execution, that source was based on repository HEAD `0ad7864` plus the reviewed four-file automatic-endpoint refinement; the refinement had not yet been committed. The deployed Lua SHA-256 was `C9FA2C9626312CA4BAD8BF579883EB7460E9397E5D1DEA2164E37DB141A42FE6`. This deployment provenance is not presented as a directly logged runtime fact.

## Raw Evidence

- Artifact: `test-artifacts/FC-004/2026-08-25/console-run-a.txt`
- Size: 586,338 bytes
- SHA-256: `81182147E8B03E7C49A377907CAB550574822EBAF6A01517D6456B0CD68D4B12`

Project Zomboid preserved this console as `console/log_4.txt` inside `C:\Users\bartk\Zomboid\logs.zip`. Its hash exactly matched the complete `console.txt` assessed immediately after Run A. The repository artifact is an unchanged extraction of that preserved archive entry.

## Bart-Supplied Execution Metadata

The following execution metadata was supplied by Bart and is not presented as directly logged observation:

- The experiment was armed between approximately 13:00 and 13:20 on the in-game watch.
- The game was allowed to run until approximately 18:20 on the in-game watch.
- Bart confirmed that he performed no UI, container, equip, or item interaction between `BEGIN` and `END`.

The in-game watch estimates are contextual operator metadata. Logged `worldHours` markers are authoritative for exact experiment duration.

## Readiness and Treatment

The final pre-experiment `READY | status=READY` snapshot at approximately `worldHours=6.15000` records:

- Group A Cooler ID `2034045827`.
- Group B Cooler ID `2076604720`.
- `FC004-A` in the primary hand.
- `FC004-B` in the secondary hand.
- `FC004-A` selected through the player-inventory binding.
- Player inventory visible, pinned, and not collapsed.
- Two steaks and zero cold packs in each Cooler.
- One fresh and one nominally frozen assigned steak in each Cooler.
- No unexpected player-inventory steaks.
- Required Build and sandbox values.

`TREATMENT_STABLE` and `BEGIN` use the same authoritative snapshot at `worldHours=6.2167253494262695`, after two stable treatment samples.

## Validity Classification

This replacement Run A is `VALID` under the accepted FC-004 criteria.

- Readiness passed before measurement.
- `FC004-A` remained selected throughout measurement.
- `FC004-A` remained primary and `FC004-B` remained secondary.
- Both Coolers remained in player inventory with unchanged contents.
- The inventory remained visible, pinned, and not collapsed.
- All 270 experiment samples report `status=RUNNING` and preserve the required treatment state.
- Sampling averaged approximately one sample per game minute across the complete interval.
- No `INVALIDATED`, harness `ERROR`, or rejected experiment marker occurred.
- Bart reported no unlogged interaction during measurement.
- `TARGET_REACHED` and `END | status=COMPLETE` occur at the same `worldHours` and contain the same endpoint state.
- No experiment sample follows `END`.

During measurement, the nominally frozen steak in selected A naturally lost its frozen state. This caused the general readiness monitor to report `reason=nominally_frozen_state_lost` after measurement had begun. It is a measured Food-state outcome, not a change to identity, context, equipment, UI treatment, or contents, and the experiment remained valid.

## Direct Runtime Observations

- `BEGIN` occurs at `worldHours=6.2167253494262695`.
- `TARGET_REACHED` and `END` occur at `worldHours=10.718294143676758`.
- Logged measurement duration is `4.501568794250488` game hours.
- The run contains 270 experiment samples, with an average interval of approximately `0.99988` game minutes.
- Every experiment sample identifies `FC004-A` as selected, with Cooler ID `2034045827`.
- Both A Food items have 270 distinct sampled `lastAged` values. The first sampled value is approximately `6.23253`; the endpoint value is approximately `10.71533`.
- Both B Food items retain the same sampled `lastAged` value, `6.147621154785156`, throughout all 270 samples.
- A's nominally frozen steak changes from `isFrozen=true`, with sampled `freezingTime` approximately `88.75` at the first sample, to `isFrozen=false` and `freezingTime=0` at the endpoint.
- B's nominally frozen steak remains logged as `isFrozen=true` with `freezingTime=94.40864562988281` throughout measurement.
- The endpoint instrumentation emits `TARGET_REACHED` immediately followed by `END | status=COMPLETE` and emits no later experiment sample.

## Test Analyst Inference

- Within this run, continuous sampled `lastAged` advancement is associated with selected `FC004-A`, while unselected `FC004-B` remains observationally stale.
- The frozen-state contrast follows the same logged split, but an unchanged stale getter value does not prove that no hidden vanilla processing occurred.
- Run A alone does not distinguish selected state from A identity or primary-hand assignment because those properties coincide in this run.

## What This Run Supports

- The automatic experiment endpoint operates correctly at the accepted 4.5-game-hour target in a substantive run.
- Sustained primary-equipped `FC004-A` selection remained observable and stable for the full measurement period.
- In this run, selected A and unselected B show a clear difference in sampled vanilla Food timing and state fields.

## What This Run Does Not Support

- It does not by itself establish that selection caused the difference.
- It does not identify the vanilla update mechanism.
- It does not establish whether stale B fields represent absent processing or processing not exposed until a later refresh opportunity.
- It does not generalize the behavior beyond this runtime, setup, duration, or UI context.
- It does not authorize architecture or production changes.

## Evidence and Reproducibility Limitations

- The selected treatment begins before `BEGIN` during readiness and stabilization, so early treatment response can precede the formal measurement marker.
- Run A confounds selected state with A identity and primary-hand assignment when considered alone.
- The runtime logs observable getter state, not an independently observable hidden vanilla scheduler.
- The exact Git revision of the endpoint refinement was not logged because the reviewed harness refinement remained uncommitted during execution.
- Bart-supplied metadata is required to establish absence of unlogged operator interaction.

## Recommended Next Owner and Action

- Test Analyst: compare this valid Run A with the counterbalanced valid Run B record.
- Researcher: after both records are canonical, determine what the counterbalanced evidence collectively establishes without treating the observed association as proof of a specific vanilla mechanism.
- Planner: update project state only after Researcher synthesis or another accepted handoff.
