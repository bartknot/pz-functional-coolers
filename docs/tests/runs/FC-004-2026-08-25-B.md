# FC-004 Run Record — 2026-08-25 — Run B

## Identity

- Task: `FC-004`
- Run date: 2026-08-25
- Run identifier: `FC-004-2026-08-25-B`
- Run role: counterbalanced Run B
- Classification: `VALID`
- Assessed by: Test Analyst
- Protocol reference: the matched, counterbalanced selected/active-versus-unselected protocol in `CURRENT_TASK.md`

## Runtime Baseline

Directly logged runtime facts:

- Project Zomboid Build `42.20.3 70207f62e0`
- `DayLength = 4`
- `FoodRotSpeed = 3`
- `FridgeFactor = 3`
- Functional Coolers Test Harness `v0.4.3-dev`
- Harness setup version `11`
- Harness control path `FC004_INVENTORY_CONTEXT_MENU`

The runtime log does not emit a Git commit. The Test Engineer separately verified that the deployed `v0.4.3-dev` Lua source matched the reviewed repository working-tree source byte-for-byte before execution. At execution, that source was based on repository HEAD `0ad7864` plus the reviewed four-file automatic-endpoint refinement; the refinement had not yet been committed. The deployed Lua SHA-256 was `C9FA2C9626312CA4BAD8BF579883EB7460E9397E5D1DEA2164E37DB141A42FE6`. This deployment provenance is not presented as a directly logged runtime fact.

## Raw Evidence

- Artifact: `test-artifacts/FC-004/2026-08-25/console-run-b.txt`
- Size: 588,538 bytes
- SHA-256: `0F2BE4A7833D8D8DD572DC2362F4FA1C3C96ACFB51E0E98E837E9A59B52674DC`

The repository artifact is an unchanged, byte-verified copy of Bart's complete `C:\Users\bartk\Zomboid\console.txt` supplied immediately after Run B.

## Bart-Supplied Execution Metadata

The following execution metadata was supplied by Bart and is not presented as directly logged observation:

- The test began at approximately 13:10 on the in-game watch.
- The game was stopped at approximately 18:20 on the in-game watch.
- Bart confirmed that he performed no additional interaction during measurement.

The in-game watch estimates are contextual operator metadata. Logged `worldHours` markers are authoritative for exact experiment duration.

## Readiness and Treatment

The final pre-experiment `READY | status=READY` snapshot at approximately `worldHours=6.18340` records:

- Group A Cooler ID `440022366`.
- Group B Cooler ID `504296563`.
- `FC004-A` in the primary hand.
- `FC004-B` in the secondary hand.
- `FC004-B` selected through the player-inventory binding.
- Player inventory visible, pinned, and not collapsed.
- Two steaks and zero cold packs in each Cooler.
- One fresh and one nominally frozen assigned steak in each Cooler.
- No unexpected player-inventory steaks.
- Required Build and sandbox values.

`TREATMENT_STABLE` and `BEGIN` use the same authoritative snapshot at `worldHours=6.21720027923584`, after two stable treatment samples.

## Validity Classification

Run B is `VALID` under the accepted FC-004 criteria.

- Readiness passed before measurement.
- `FC004-B` remained selected throughout measurement while secondary-equipped.
- `FC004-A` remained primary and `FC004-B` remained secondary.
- Both Coolers remained in player inventory with unchanged contents.
- The inventory remained visible, pinned, and not collapsed.
- All 270 experiment samples report `status=RUNNING` and preserve the required treatment state.
- Sampling averaged approximately one sample per game minute across the complete interval.
- No `INVALIDATED`, harness `ERROR`, or rejected experiment marker occurred.
- Bart reported no unlogged interaction during measurement.
- `TARGET_REACHED` and `END | status=COMPLETE` occur at the same `worldHours` and contain the same endpoint state.
- No experiment sample follows `END`.

During measurement, the nominally frozen steak in selected B naturally lost its frozen state. This caused the general readiness monitor to report `reason=nominally_frozen_state_lost` after measurement had begun. It is a measured Food-state outcome, not a change to identity, context, equipment, UI treatment, or contents, and the experiment remained valid.

## Direct Runtime Observations

- `BEGIN` occurs at `worldHours=6.21720027923584`.
- `TARGET_REACHED` and `END` occur at `worldHours=10.717742919921875`.
- Logged measurement duration is `4.500542640686035` game hours.
- The run contains 270 experiment samples, with an average interval of approximately `0.99987` game minutes.
- Every experiment sample identifies `FC004-B` as selected, with Cooler ID `504296563`.
- Both B Food items have 270 distinct sampled `lastAged` values. The first sampled value is approximately `6.23202`; the endpoint value is approximately `10.71478`.
- Both A Food items retain the same sampled `lastAged` value, `6.169121742248535`, throughout all 270 samples.
- B's nominally frozen steak changes from `isFrozen=true`, with sampled `freezingTime` approximately `87.80` at the first sample, to `isFrozen=false` and `freezingTime=0` at the endpoint.
- A's nominally frozen steak remains logged as `isFrozen=true` with `freezingTime=91.99327087402344` throughout measurement.
- The endpoint instrumentation emits `TARGET_REACHED` immediately followed by `END | status=COMPLETE` and emits no later experiment sample.

## Test Analyst Inference

- Within this run, continuous sampled `lastAged` advancement is associated with selected `FC004-B`, while unselected `FC004-A` remains observationally stale.
- The frozen-state contrast follows the same logged split, but an unchanged stale getter value does not prove that no hidden vanilla processing occurred.
- Because B is selected while secondary-equipped and A is unselected while primary-equipped, this run demonstrates that a selected secondary Cooler can show the continuous observed update pattern while an unselected primary Cooler remains stale.

## Counterbalanced Run-Level Comparison

The two valid records show a mirrored observational result:

| Run | Primary | Secondary | Selected | Continuously advancing sampled `lastAged` | Stale sampled `lastAged` |
| --- | --- | --- | --- | --- | --- |
| A | FC004-A | FC004-B | FC004-A | FC004-A | FC004-B |
| B | FC004-A | FC004-B | FC004-B | FC004-B | FC004-A |

At experiment level, the observed update pattern follows selected identity across the counterbalanced runs. Fixed A/B identity and primary/secondary assignment alone do not explain the mirrored result. Researcher owns any broader cross-run empirical conclusion and the confidence assigned to it.

## What This Run Supports

- Sustained secondary-equipped `FC004-B` selection remained observable and stable for the full measurement period.
- In this run, selected B and unselected A show a clear difference in sampled vanilla Food timing and state fields.
- Together with valid Run A, the run-level result is counterbalanced across selected identity while hand assignments remain fixed.

## What This Run Does Not Support

- It does not identify the vanilla update mechanism.
- It does not prove whether stale unselected values represent absent processing or delayed observable refresh.
- It does not establish that every selected container or every Project Zomboid context behaves identically.
- It does not generalize the behavior beyond this runtime, setup, duration, or UI context.
- It does not authorize architecture or production changes.

## Evidence and Reproducibility Limitations

- The selected treatment begins before `BEGIN` during readiness and stabilization, so early treatment response can precede the formal measurement marker.
- The runtime logs observable getter state, not an independently observable hidden vanilla scheduler.
- The exact Git revision of the endpoint refinement was not logged because the reviewed harness refinement remained uncommitted during execution.
- Bart-supplied metadata is required to establish absence of unlogged operator interaction.
- Two valid runs provide one counterbalanced pair; replication is required before broad generalization.

## Recommended Next Owner and Action

- Researcher: synthesize valid Run A and valid Run B, determine how strongly the counterbalanced evidence supports a selected/active refresh effect, and preserve the distinction between observable refresh and hidden processing.
- Planner: update project state and sequence any replication or production consequence only after Researcher synthesis.
- No architecture or production implementation follows automatically from these run records.
