# FC-003 Run Record — 2026-08-22

## Identity

- Task: `FC-003`
- Run date: 2026-08-22
- Run identifier: `FC-003-2026-08-22`
- Classification: `VALID`
- Assessed by: Test Analyst
- Execution protocol: the standardized carried-Cooler intervention accepted in commit `cb4cb17`
- Classification criteria: the material validity criteria accepted in commit `b3d1237`

The run was initially assessed against the stricter pre-`b3d1237` wording. Bart then explicitly revised and accepted the protocol criteria so that a complete `phase=START` snapshot may establish final readiness and primary/secondary hand assignment is execution metadata rather than a validity criterion. This record preserves that provenance and applies the canonical revised criteria.

## Runtime Baseline

Directly logged runtime facts:

- Project Zomboid Build `42.20.3`
- `DayLength = 4`
- `FoodRotSpeed = 3`
- `FridgeFactor = 3`
- Functional Coolers Test Harness `v0.3.7-dev`
- Functional Coolers Prototype `v0.3.0-dev`
- Diagnostic marker `diagnostics=FRIDGE_CONTEXT`
- Harness status `[FCTH] status=READY_TO_DISTRIBUTE`

The deployed Git commit was not emitted by the runtime and is not independently verified by the log. The protocol commits above describe the canonical assessment context, not a claim that the game logged its deployed repository revision.

## Raw Evidence

- Artifact: `test-artifacts/FC-003/2026-08-22/console.txt`
- Size: 1,886,976 bytes
- SHA-256: `9318F87B47836426F1AEEF201C8C23DC6414BC274E435F677BA62CC0D3925BE6`

The imported artifact is an unchanged, byte-verified copy of Bart's complete preserved console log.

## Bart-Supplied Execution Metadata

The following statement was supplied by Bart and is not presented as directly logged observation:

- The test was executed completely according to the active FC-003 protocol.
- No protocol deviation or additional test-item manipulation was reported.

Where the diagnostic sampling does not show an individual UI action, this statement supports execution provenance but does not convert an unlogged action into a directly logged observation.

## Final Readiness Evidence

The authoritative `phase=START` snapshot begins around `worldHours=6.18340` and satisfies the revised readiness gate:

- P0: `CARRIED`, 2 steaks, 0 cold packs.
- P1: `CARRIED`, 2 steaks, 1 cold pack.
- P2: `CARRIED`, 2 steaks, 2 cold packs.
- P4: `CARRIED`, 2 steaks, 4 cold packs.
- P1G: `GROUND`, 2 steaks, 1 cold pack.
- CONTROL: one fresh and one nominally frozen steak in player inventory.
- FRIDGE: powered refrigerator, one fresh and one nominally frozen steak.
- The per-Food start records confirm one fresh and one nominally frozen steak for each Cooler group.

Earlier `MISSING` or `WAITING` records describe pre-distribution or pre-placement setup state. Under the revised canonical criteria they do not override the later complete start snapshot.

## Validity Classification

This run is `VALID` under the material FC-003 criteria accepted in `b3d1237`.

- Calibration values match the required baseline.
- The final `phase=START` snapshot passes readiness for every group.
- Bart reports complete A/B/C/D execution without unreported manipulation.
- Phase B directly records the required P4/P2 pairing followed by the required P1/P0 pairing.
- Phase D directly records the required P4/P2 pairing.
- Phase D uses P2 primary and P4 secondary. This is execution metadata and is explicitly non-invalidating under the revised criteria.
- No relevant Functional Coolers or harness exception was found.

The log does not sample every later Phase D UI action. That limits direct reconstruction and the conclusions available from the final intervention window, but it is not a demonstrated protocol violation given Bart's execution metadata.

## Direct Runtime Observations

### First waiting and intervention window

- P0, P1, P2, P4, and CONTROL show `lastAged` catch-up around `worldHours=8.18317`.
- The first carried-Cooler equip snapshot follows around `worldHours=8.20005`.
- P4/P2 are then observed as the first equipped pair.
- P1/P0 are subsequently observed as the second equipped pair around `worldHours=8.25003`.
- P1G shows catch-up with `lastAged` around `8.24505`.
- FRIDGE shows catch-up with `lastAged` around `8.24989`.

### Second waiting and intervention window

- P0, P1, P2, P4, and CONTROL show `lastAged` catch-up around `worldHours=10.18312`.
- The Phase D P4/P2 pairing is first sampled around `worldHours=10.20007`, with P2 primary and P4 secondary.
- The final diagnostic sample around `worldHours=10.21674` still shows that pairing.
- At that final sample, P1G and FRIDGE retain approximately 1.97 and 1.94 game hours of `lastAged` lag respectively; a later second-window catch-up for those groups is not captured before measurement diagnostics end.

### Errors

- No `[FCTH-ERROR]` is present.
- No Lua exception attributable to Functional Coolers or the test harness is present.
- Unrelated vanilla warnings during shutdown/menu transition are not treated as Functional Coolers test failures.

## Test Analyst Inference

- In both waiting windows, the sampled `lastAged` catch-up for P0, P1, P2, P4, and CONTROL precedes the first sampled carried-Cooler equip/inspection intervention.
- The later inspection therefore is not a necessary explanation for those specific catch-up events in this run.
- The staggered first-window P1G and FRIDGE catch-ups are temporally near later intervention activity, but the run does not isolate UI access, normal vanilla scheduling, or another update path as their cause.
- Because Phase D ends before later P1G/FRIDGE outcomes are sampled, the run does not establish whether or when those groups would catch up in the second window.

## Unsupported Causal Explanations

This run does not establish:

- the specific vanilla event or scheduler responsible for `lastAged` catch-up;
- that opening, equipping, selecting, or inspecting a Cooler causes catch-up;
- that primary and secondary hands are behaviorally identical in every Project Zomboid subsystem;
- that P1G or FRIDGE follows the same second-window timing as the carried groups;
- a universal refresh rule beyond this assessed run.

## What This Run Supports

- A valid FC-003 run can use the complete `phase=START` snapshot as material readiness evidence.
- In this run, P0, P1, P2, P4, and CONTROL show catch-up before the subsequent sampled inspection intervention in both waiting windows.
- The subsequent inspection is therefore not required to explain those particular carried/CONTROL catch-ups.
- The standardized pairings are practically executable without dropping the carried Coolers to the ground.

## What This Run Does Not Support

- It does not identify the causal vanilla refresh mechanism.
- It does not prove that inspection never has an additional effect.
- It does not provide a complete sampled Phase D outcome for P1G and FRIDGE.
- It does not establish a replicated cross-run general rule.
- It does not by itself authorize architecture or production changes.

## Evidence and Reproducibility Limitations

- Phase boundaries are inferred from `worldHours`, context transitions, and Bart's execution metadata rather than explicit A/B/C/D log markers.
- The diagnostic sampler does not capture every UI selection or later Phase D action.
- The two waiting periods are approximate rather than bounded by explicit phase-start and phase-end markers.
- The deployed repository commit is not logged by the runtime.
- Primary/secondary hand assignment is directly visible for sampled pairings, while exact inspection clicks are not consistently represented by `containerActive` diagnostics.
- The final sampled window ends before a second P1G/FRIDGE catch-up is observed.

## Unresolved Experiment-Level Questions

- Which vanilla update path refreshes carried Cooler Food and CONTROL before inspection?
- Does inspection have any additional effect beyond the pre-intervention catch-up observed here?
- Under a longer post-Phase-D observation window, when do P1G and FRIDGE update?
- Would another valid replication reproduce the same ordering and timing?

## Recommended Next Owner and Action

- Researcher: compare this valid run with the canonical invalid run from 2026-08-21 and determine only what the assessed evidence collectively supports.
- Planner: after Researcher synthesis, update project state and sequence any justified follow-up experiment or specialist task.
- No architecture or production implementation should begin solely from this run record.
