# FC-006 Run Record — 2026-08-26

## Identity

- Task: `FC-006`
- Run date: 2026-08-26
- Run identifier: `FC-006-2026-08-26`
- Classification: `VALID`
- Assessed by: Test Analyst
- Protocol reference: the accepted Build 42.20.4 managed-to-vanilla Food handoff protocol in `CURRENT_TASK.md`
- Runtime path: substantive experiment, `mode=EXPERIMENT`

## Runtime Baseline

Directly logged runtime facts:

- Project Zomboid Build `42.20.4 b0bbce05d5`
- `DayLength = 4`
- `FoodRotSpeed = 3`
- `FridgeFactor = 3`
- Functional Coolers Test Harness `v0.5.2-dev`
- Harness setup version `14`
- Enabled mod set: `FunctionalCoolersTestHarness`
- Production Functional Coolers mod disabled
- Zero unexpected enabled mods and zero unexpected test items

The runtime log does not emit a Git commit. `CURRENT_TASK.md` records that setup 14 was the accepted, deployed four-file Build 42.20.4 harness state after the marker-only Test Engineer adaptation and passed infrastructure smoketest. That planning provenance is not presented as a directly logged runtime fact.

## Raw Evidence

- Artifact: `test-artifacts/FC-006/2026-08-26/console.txt`
- Size: 193,679 bytes
- SHA-256: `092D61159C2C57A0162A4E941FD57F011D1B51A022BEB262F4654BA91C7218D7`

The repository artifact is an unchanged, byte-verified copy of Bart's complete `C:\Users\bartk\Zomboid\console.txt`. Source and target size and SHA-256 matched at import.

## Bart-Supplied Execution Metadata

The following execution metadata was supplied by Bart and is not presented as directly logged observation:

- Bart confirmed that he performed no UI, container, equip, or item interaction from `CONTROL | status=ARMED` through automatic `END`.
- Bart confirmed the combined operator question that also required normal speed without fast-forward.

The log independently records the required UI, equipment, container, and item state at every experiment sample. Bart's statement addresses possible unlogged interaction between samples.

## Readiness and Treatment

The harness emitted `MATCHED_SETUP_CREATED` at `worldHours=2.0000743865966797` and a final pre-run `READY | status=READY` marker at `worldHours=2.116692543029785`.

The run preserved one stable identity and treatment assignment:

- `FC006-GUARD` Cooler ID `984846483`, primary hand and selected through the player-inventory binding;
- `FC006-TEST` Cooler ID `1012071938`, secondary hand and unselected;
- V Steak ID `1462015840`;
- A Steak ID `875160373`;
- U Steak ID `992537179`;
- player inventory visible, pinned, and not collapsed;
- required contents, contexts, hand assignment, and selected treatment ready at all 102 experiment records.

`CONTROL | status=ARMED` occurs at `worldHours=2.1950149536132812`. Two stable `ARMED_SAMPLE` records precede `BEGIN` at `worldHours=2.2167348861694336`.

## Validity Classification

This run is `VALID` under the accepted FC-006 criteria.

- Exactly one substantive experiment path is present.
- All 102 experiment records report `evidenceEligible=true`.
- Build, harness/setup identity, sandbox values, enabled-mod boundary, content identity, equipment, selection, and UI state remain correct throughout the run.
- The marker sequence is complete: `CONTROL`, two stable `ARMED_SAMPLE` records, `BEGIN`, `PRE_HANDOFF`, `HANDOFF_APPLIED`, `HANDOFF_COMMITTED`, `UPDATE_1`, `UPDATE_2`, `UPDATE_3`, and automatic `END`.
- No `INVALIDATED`, rejected experiment action, harness failure, failed API call, or unexpected item/mod condition occurs.
- The getter-only stale interval is `1.0000343322753906` game hours, within the `0.001`-hour tolerance.
- Handoff operations have timestamp spread `0` and all required calls pass.
- Before `HANDOFF_COMMITTED`, V, A, and U have exactly equal projected age, heat, and freezing time, with exactly equal required phase flags: `frozen=false`, `freezing=false`, and `thawing=true`.
- All three scheduled updates occur within the `0.001`-hour timing tolerance and have callback spread `0`.
- V and A agree exactly in every observed numeric and public phase field after each scheduled update.
- U shows the predeclared stale-cursor sensitivity in both age and freezing state.
- Bart reports no unlogged interaction or fast-forward during the run.
- Automatic `END | status=COMPLETE | completionReason=three_scheduled_updates_complete` is present.

The previously accepted setup-14 infrastructure smoketest supplied the separate prerequisite thaw-response validation. It is not empirical evidence from this run.

## Direct Runtime Observations

At `BEGIN`, V, A, and U share `age=0.5`, `heat=1`, `freezingTime=80`, the required phase flags, and `lastAged=2.2167348861694336`.

At handoff, all projected public values and flags remain equal. V and A align `lastAged` to the handoff time while U intentionally retains the stale value:

| Marker | `worldHours` | V/A age | U age | V/A freezingTime | U freezingTime | V/A `lastAged` | U `lastAged` |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `HANDOFF_COMMITTED` | 3.2167692184 | 0.5000000 | 0.5000000 | 80.0000 | 80.0000 | 3.2167692184 | 2.2167348862 |
| `UPDATE_1` | 3.3834667206 | 0.5069457 | 0.5486138 | 68.8868 | 2.2179 | 3.3834667206 | 3.3834667206 |
| `UPDATE_2` | 3.5501613617 | 0.5138913 | 0.5555595 | 57.7739 | 0.0000 | 3.5501613617 | 3.5501613617 |
| `UPDATE_3` | 3.7167863846 | 0.5208340 | 0.5625021 | 46.6655 | 0.0000 | 3.7167863846 | 3.7167863846 |

Additional direct observations:

- Handoff uses `updateAge()` then public projection for V, `setAutoAge()` then public projection for A, and public projection only for U.
- `UPDATE_1` occurs `0.0000308355` game hour after its scheduled time; `UPDATE_2` is `0.0000588099` late; `UPDATE_3` is `0.0000171661` late.
- At `UPDATE_1`, U age exceeds V/A by `0.0416681170`, while U freezing time is `66.6689605713` lower.
- At `UPDATE_2`, U has reached `freezingTime=0` and `thawing=false`; V and A remain equal at `freezingTime=57.7738571167` and `thawing=true`.
- V and A remain exactly equal through `UPDATE_3`; U retains the age offset and remains fully thawed.
- Heat remains `1` for all groups at every recorded protocol marker.

## Test Analyst Interpretation

- The run demonstrates the intended evidence contrast. Public projection alone did not align U with V; the stale interval was processed at the first explicit post-handoff `updateAge()` opportunity.
- A, which received `setAutoAge()` followed by the final public projection, matched V exactly at handoff and through three later explicit `updateAge()` opportunities.
- Within this run, no duplicate stale-hour processing or omitted post-handoff progression is observable for A relative to the V reference.
- The U divergence establishes that the protocol was capable of detecting the stale-cursor failure mode rather than merely producing three indistinguishable groups.

These are run-level interpretations. They do not independently establish a universal or production-safe Project Zomboid API contract.

## What This Run Supports

- In the exact tested Build 42.20.4 scenario, `setAutoAge()` followed by the final public projection is an empirically supported candidate for aligning vanilla Food timing at managed-to-vanilla handoff.
- The candidate A sequence produces the same observed age, heat, freezing time, public phase flags, and `lastAged` progression as the V reference across three ten-game-minute update opportunities.
- Public projection without timing alignment is insufficient under the same setup and produces a clearly detectable stale-interval catch-up.
- The FC-006 evidence stream distinguishes the intended aligned and unaligned outcomes.

## What This Run Does Not Support

- It does not establish universal safety across Food classes, thermal phases, contexts, durations, saves, transfers, or Project Zomboid builds.
- It does not expose or directly verify the private freezing-update cursor.
- It does not establish when vanilla would naturally schedule the first update; the protocol supplies explicit verified `updateAge()` opportunities.
- It does not establish that `setAutoAge()` has no other unobserved side effect.
- It does not independently validate the Functional Coolers model's projected values; it tests handoff continuity after a deterministic projection.
- It does not authorize production implementation or silently revise `docs/ARCHITECTURE.md`.

## Evidence and Reproducibility Limitations

- This is one valid run on one fresh save with one `Base.Steak` per group.
- The tested context is an unselected carried and secondary-equipped `FC006-TEST` Cooler while a separate primary-equipped guard Cooler remains selected in a visible, pinned player inventory.
- Post-handoff processing is invoked explicitly three times rather than observed through an uncontrolled natural scheduler.
- Public getters and `lastAged` are observable; the private freezing timing cursor is inferred through controlled response rather than logged directly.
- The runtime artifact does not identify the deployed Git commit.
- Bart-supplied metadata is required to exclude interaction between logged samples.

## Recommended Next Owner and Action

- Researcher: determine the bounded task-level conclusion from this valid run and the accepted feasibility evidence without generalizing beyond the tested scope.
- Planner: after Researcher synthesis, record whether FC-006 is complete and sequence the architecture or implementation consequence.
- Architect: if Planner routes the result to architecture, determine how the bounded verified sequence affects P-4 without assuming untested generality.
- Coder: no production work is authorized by this run.

