# FC-003 Run Record — 2026-08-21

## Identity

- Task: `FC-003`
- Run date: 2026-08-21
- Run identifier: `FC-003-2026-08-21`
- Classification: `INVALID`
- Assessed by: Test Analyst
- Protocol reference: the FC-003 A/B/C/D protocol in `CURRENT_TASK.md` as accepted when the run was executed, before the carried-Cooler intervention was revised

## Runtime Baseline

Directly logged runtime facts:

- Project Zomboid Build `42.20.3`
- `DayLength = 4`
- `FoodRotSpeed = 3`
- `FridgeFactor = 3`
- Functional Coolers Test Harness `v0.3.7-dev`
- Functional Coolers Prototype `v0.3.0-dev`
- Diagnostic marker `diagnostics=FRIDGE_CONTEXT`
- Harness status `[FCTH] status=READY_TO_DISTRIBUTE`

The deployed Git commit was not recorded in the console log and is therefore not independently verified by this run record.

## Raw Evidence

- Artifact: `test-artifacts/FC-003/2026-08-21/console.txt`
- Size: 1,865,861 bytes
- SHA-256: `A7507EBDBFE43105A151493A2A4ACADA2CC5B9A6C159D5C6D35B26163DF93722`

The artifact is the complete preserved console log. Project Zomboid appended normal shutdown lines on 2026-08-22 before the final artifact was imported. The imported artifact is an unchanged copy of that finalized source file; its shutdown lines do not add another experiment phase.

## Bart-Supplied Execution Metadata

The following is execution metadata supplied by Bart. It is not presented as if directly observed in the console log.

- Before measurement, Bart checked that the Cooler and refrigerator contents were distributed correctly while the Coolers were still on the ground.
- Bart then placed P0, P1, P2, and P4 in player inventory. P1G remained on the ground.
- NEUTRAL was selected during both waiting periods, each lasting approximately two game hours.
- For each inspection period, Bart dropped P0, P1, P2, and P4 from player inventory onto the ground so all four could be inspected consistently, then returned them to player inventory after the first inspection period.
- Bart used the debug play/pause control so successive actions occurred close together while still allowing the short runtime intervals required for the actions themselves.
- No steak or cold pack was individually moved, consumed, or modified during the measurement periods.
- Bart explained that a Cooler item carried in player inventory cannot be directly opened for inspection without first equipping it or moving it into another inspectable context. Four carried Coolers cannot all be equipped simultaneously because only two hand slots are available.

## Validity Classification

This run is `INVALID` for the original FC-003 A/B/C/D causal question.

### Invalidating reason 1: prescribed context was altered

The accepted protocol prohibited moving, equipping, transferring, or otherwise manipulating the carried Coolers during Phase B and Phase D. Bart transferred P0, P1, P2, and P4 between `CARRIED` and `GROUND` for both inspection periods. Dropping, picking up, and the associated context transitions are therefore competing interventions that cannot be separated from UI/container inspection.

### Invalidating reason 2: canonical readiness gate was not recorded

The protocol required P0, P1, P2, P4, P1G, CONTROL, and FRIDGE all to report `READY` immediately before Phase A. The log records `READY_TO_DISTRIBUTE` and subsequently records the expected groups and contents at the diagnostic start, but it does not contain the required explicit all-groups `READY` gate in the prescribed form. Bart's manual setup check does not replace that canonical logged gate.

Either reason is sufficient to prevent this run from supporting the intended causal conclusion.

## Direct Runtime Observations

The following observations come directly from the preserved console log.

- The diagnostic run starts around `worldHours=6.61904` with P0, P1, P2, and P4 reported as `CARRIED`, P1G as `GROUND`, CONTROL in player inventory, and FRIDGE in the powered refrigerator context.
- Immediately before the first inspection window, most groups had accumulated approximately 1.87–1.94 game hours of `lastAgedLagHours`.
- P0, P1, P2, P4, and CONTROL show `lastAged` catch-up around `worldHours=8.51665`.
- P1G shows catch-up around `worldHours=8.53316`.
- FRIDGE reports `lastAged` around `8.54372`, observed in diagnostics around `worldHours=8.55005`.
- Around `worldHours=10.61664`, P1G and CONTROL show another catch-up.
- The log ends before an equivalent second-window outcome can be established for every other group.
- Sampled context diagnostics report the four carried groups as `CARRIED` and do not capture them in a hand or on the ground. The short drop/inspect/pickup transitions described by Bart occurred between or outside the sampled diagnostic snapshots.
- No `[FCTH-ERROR]` or Lua exception attributable to the harness or Functional Coolers was found in the run log.

## Test Analyst Inference

- The first inspection period is temporally associated with staggered catch-up observations across the test groups and controls.
- The diagnostic sampling interval was not sufficient to reconstruct every short-lived Cooler context transition described by Bart.
- Because UI inspection, dropping, picking up, context transitions, debug play pulses, and vanilla update processing occurred in the same intervention window, the log cannot isolate which event or combination of events was responsible for a catch-up.
- Because the log ends during the second observation window, absence of a recorded second catch-up for a group is not evidence that no later catch-up occurred.

## Unsupported Causal Explanations

This run does not establish that any of the following caused the observed catch-up:

- opening or selecting a specific Cooler;
- dropping a Cooler;
- picking up a Cooler;
- changing between `CARRIED` and `GROUND`;
- selecting the ground or refrigerator context;
- a debug play/pause pulse;
- a general vanilla Food update cycle.

The available evidence does not distinguish these explanations under the executed procedure.

## What This Run Supports

- The configured runtime, harness, and Functional Coolers diagnostics produced a complete analyzable console artifact without a relevant harness exception.
- The log contains narrower diagnostic observations about `lastAged`, context snapshots, and catch-up timing.
- Bart's execution metadata identifies a practical defect in the original protocol: carried Cooler inspection required an additional equip or transfer intervention.
- The invalid run materially informed the later decision to standardize carried-Cooler equipping in Phase B and Phase D.

## What This Run Does Not Support

- It does not answer the original FC-003 causal question about UI/container inspection alone.
- It does not establish a general Project Zomboid refresh rule.
- It does not distinguish UI access from Cooler movement, context changes, or vanilla update scheduling.
- It does not demonstrate that all required groups passed the canonical readiness gate immediately before Phase A.
- It does not establish a complete second-window outcome for every group.

## Evidence and Reproducibility Limitations

- The required all-groups `READY` event is absent from the log.
- Phase boundaries were not emitted as explicit A/B/C/D markers.
- The wait periods were approximately, rather than explicitly bounded by recorded phase-start and phase-end `worldHours` values.
- Short Cooler transfers were not captured by the sampled context diagnostics.
- The deployed repository commit is not recorded in the runtime artifact.
- The second observation window ends before all groups have a conclusive recorded outcome.
- Bart-supplied execution metadata is necessary to reconstruct the material protocol deviation.

## Unresolved Experiment-Level Questions

- Under the revised standardized equip intervention, which groups show catch-up and at what logged times during Phase B and Phase D?
- Can the next run record the canonical all-groups `READY` gate immediately before Phase A?
- Can explicit phase timing and the fixed equip sequence make access-related observations reproducible without conflating them with unrecorded transfers?

## Recommended Next Owner and Action

- Planner: the practical protocol defect has been addressed by the accepted standardized carried-Cooler equip sequence in the current FC-003 protocol.
- Test Analyst: assess a new FC-003 run executed under that revised protocol, with the canonical readiness gate and complete evidence preserved.
- Researcher: only after assessed run evidence exists, determine what the available experiment or experiments collectively establish; do not use this invalid run to support the original causal conclusion.
